# R2 Setup

Bucket name: `lopaka-chameleon-levels`

Object layout:

- `scenes/{level_id}.webp`
- `masks/{level_id}.png`

Scene objects are served through the API Worker route:

- `GET /assets/scenes/:file`

Mask objects must remain private and must only be read by the API Worker during guess validation.
